<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <template id="auth_signup_fields_extend" name="Registration Fields Extend">
            <div class="form-group field-company_name">
                <label for="company_name" class="control-label">company_name</label>
                <input type="text" name="company_name" t-att-value="company_name" id="company_name" class="form-control"
                       required="required"/>
            </div>

            <div class="form-group field-vat">
                <label for="vat" class="control-label">vat</label>
                <input type="text" name="vat" t-att-value="vat" id="vat" class="form-control" required="required"/>
            </div>

            <div class="form-group field-fiscal_code">
                <label for="fiscal_code" class="control-label">Fiscal code</label>
                <input type="text" name="fiscal_code" t-att-value="fiscal_code" id="fiscal_code" class="form-control"
                       required="required"/>
            </div>


            <div class="form-group field-email">
                <label for="email" class="control-label">Email</label>
                <input type="text" name="email" t-att-value="email" id="email" class="form-control"
                       required="required"/>
            </div>

            <div class="form-group field-phone">
                <label for="phone" class="control-label">Phone</label>
                <input type="text" name="phone" t-att-value="phone" id="phone" class="form-control"
                       required="required"/>
            </div>
            <div class="form-group field-city">
                <label for="city" class="control-label">City</label>
                <input type="text" name="city" t-att-value="city" id="city" class="form-control" required="required"/>
            </div>

            <div class="form-group field-zip">
                <label for="zip" class="control-label">Zip</label>
                <input type="text" name="zip" id="zip" class="form-control" required="required"/>
            </div>

            <div class="form-group field-street_name">
                <label for="street" class="control-label">Street</label>
                <input type="text" name="street" t-att-value="street" id="street" class="form-control"
                       required="required"/>
            </div>


            <div class="form-group field-state">
                <label for="state_id" class="control-label">State</label>
                <select name="state_id" class="form-control" required="required">
                    <option value="">Select</option>
                    <t t-foreach="states or []" t-as="state">
                        <option t-att-value="state.id">
                            <t t-esc="state.name"/>
                        </option>
                    </t>
                </select>
            </div>
            <div class="form-group field-country">
                <label for="country_id" class="control-label">Country</label>
                <select name="country_id" class="form-control" required="required">
                    <option value="">Select</option>
                    <t t-foreach="countries or []" t-as="country">
                        <option t-att-value="country.id">
                            <t t-esc="country.name"/>
                        </option>
                    </t>
                </select>
            </div>
            <div class="form-group field-pec">
                <label for="pec" class="control-label">PEC</label>
                <input type="text" name="pec" t-att-value="pec" id="pec" class="form-control" required="required"/>
            </div>

            <div class="form-group field-sdi">
                <label for="sdi" class="control-label">SDI</label>
                <input type="text" name="sdi" t-att-value="sdi" id="sdi" class="form-control" required="required"/>
            </div>
        </template>
    </data>
</odoo>
